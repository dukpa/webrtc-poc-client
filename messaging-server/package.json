{
  "name": "messaging-server",
  "version": "0.0.1",
  "description": "Messaging server for the web rtc poc",
  "main": "./build/index.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "dev": "nodemon --watch src -e ts,ejs --exec npm start"
  },
  "author": "Jigme Dukpa",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/node": "^14.11.10",
    "nodemon": "^2.0.5",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
